image: golang:1.17-alpine

cache:
  paths:
    - /go/src

stages:
  - test
  - build
  - release

test:
  stage: test
  script:
    - cd $CI_PROJECT_DIR
    - apk add build-base
    - make dep
    - make test_with_coverage
  artifacts:
    when: always
    reports:
      junit: report.xml

build:
  stage: build
  only:
    - master
  script:
    - cd $CI_PROJECT_DIR
    - apk add build-base git upx
    - make dep
    - make
    - make optimize
  artifacts:
    paths:
      - envpop-linux

release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  only:
    - tags
  script:
    - echo "Release job"
  release:
    name: 'Release $CI_COMMIT_TAG'
    description: 'Release created automatically using release-cli.'
    tag_name: '$CI_COMMIT_SHORT_SHA'
    assets:
      links:
        - name: 'Linux binary'
          url: 'https://gitlab.com/mr_timp/envpop/-/jobs/${GE_JOB_ID}/artifacts/file/dist/envpop-linux'
